// const { getKey } = require('../controller');

describe('Generate a random key for the game', () => {
  it('should return a key with adjetive-noun format', async() => {
    // console.log('test');
  });
});
