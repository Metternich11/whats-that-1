const Actions = require('../redux/actions');

const gameModel = {

  addGame: (key, id) => {
    return true;
  },

  addPlayer: (message, id) => {
    return true;
  }
};

module.exports = gameModel;

