import styled from "styled-components";

const ImageCarousel = styled.div`
  display: flex;
  overflow: hidden;
  overflow-x: scroll;
  width: 100%;
  margin: 0 0 4vh 0;
  text-align: center;
`;

export default ImageCarousel;
