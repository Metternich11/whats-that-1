import styled from "styled-components";

const PlayerHasSolvedRound = styled.img`
  width: 20px;
  background-color: #ffffff;
  border-radius: 50%;
  color: #000000;
  box-shadow: 0 7px 14px rgba(50, 50, 93, 0.1), 0 3px 6px rgba(0, 0, 0, 0.08);
`;

export default PlayerHasSolvedRound;
