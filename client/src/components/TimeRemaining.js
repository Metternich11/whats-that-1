import styled from "styled-components/macro";

const TimeRemaining = styled.h3`
  color: #fff;
  font-size: 1.3rem;
  font-weight: 200;

  h2,
  p {
    margin: 0;
  }
`;

export default TimeRemaining;
